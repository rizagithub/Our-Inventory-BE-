const dotenv = require('dotenv');
const path = require('path');

dotenv.config()

module.exports = {
    // ini agar yang kebaca di config itu database.js
    config: path.resolve('config', 'database.js')
}